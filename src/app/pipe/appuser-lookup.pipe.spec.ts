import { AppuserLookupPipe } from './appuser-lookup.pipe';

describe('AppuserLookupPipe', () => {
  it('create an instance', () => {
    const pipe = new AppuserLookupPipe();
    expect(pipe).toBeTruthy();
  });
});
